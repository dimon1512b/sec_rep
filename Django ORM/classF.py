"""
from django.db.models import F

Клас F дозволяє використовувати значення поля того самого об'єкта:

objects = Table.objects.filter(pk=F('field'))
#  pk=F('field') тобто тут шукається такий об'єкт у якого значення первинного
#  ключа співпадає з значенням якогось поля

Більш реальний приклад ось:

Table.objects.filter(slug='value').update(views=F('views') + 1)
Тут ми зробили інкремент якогось поля не залежно від його попереднього значення

"""